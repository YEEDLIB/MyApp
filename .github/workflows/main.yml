name: Build Tauri App (Windows Only)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-windows:
    runs-on: windows-latest

    defaults:
      run:
        shell: bash
        working-directory: MyApp   # ✅ KU BADAL HADDII FOLDER-KAAGA KALE YAHAY

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Verify Rust
        run: |
          rustc --version
          cargo --version

      - name: Install frontend dependencies
        run: npm install

      - name: Build Tauri App (Windows)
        uses: tauri-apps/tauri-action@v0
        with:
          projectPath: MyApp   # ✅ KU BADAL HADDII FOLDER-KAAGA KALE YAHAY
          tagName: app-v__VERSION__
          releaseName: "App v__VERSION__"
          releaseBody: "Windows build from GitHub Actions"
          releaseDraft: false
          prerelease: false

      - name: Upload Windows Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: tauri-windows-build
          path: MyApp/src-tauri/target/release/bundle/
